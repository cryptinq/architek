from __future__ import annotations
from typing import List

from app.common.entity.%ENTITY% import %ENTITY%
from core.orm.entity.repository.BaseRepository import BaseRepository


class %ENTITY%Repository(BaseRepository):

    def __init__(self, entity: %ENTITY%): super().__init__(entity)

    def get_by_id(self, entity_id: int) -> %ENTITY% | List[%ENTITY%]: return super().get_by_id(entity_id)
